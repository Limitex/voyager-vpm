# Voyager Environment Variables

# Output path for generate command (default: index.json)
# VOYAGER_OUTPUT_PATH=index.json

# Fetch command options

# GitHub token for API access (optional, increases rate limit)
# VOYAGER_GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Name of the asset file to download from releases (default: package.json)
# VOYAGER_ASSET_NAME=package.json

# Shared options (fetch & validate)

# Maximum number of concurrent operations (default: 5, range: 1-50)
# VOYAGER_MAX_CONCURRENT=5

# Maximum number of retries for failed requests (default: 3)
# VOYAGER_MAX_RETRIES=3

# Disable colored output (standard: https://no-color.org/)
# NO_COLOR=1
